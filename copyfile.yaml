- name: Copy file to host
  hosts: 192.168.1.200
  gather_facts: false
  vars:
    src: /home/navya/Documents/virus.txt
    dest: /home/aviksha/Documents/virus.txt
  tasks:
    - name: Copy file to remote host
      synchronize:
        src: "{{ src }}"
        dest: "{{ dest }}"
